local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()

local Options = Library.Options
local Toggles = Library.Toggles

Library.ForceCheckbox = false
Library.ShowToggleFrameInKeybinds = true

local Window = Library:CreateWindow({
    Title = "Made by MasterZ",
    Footer = "v1.0.0",
    Icon = 0,
    NotifySide = "Right",
    ShowCustomCursor = true,
})

local Tabs = {
    Main = Window:AddTab("Main", "user"),
}

-- GroupBox 1 (Kiri)
local GroupBox1 = Tabs.Main:AddLeftGroupbox("Main Options1")

GroupBox1:AddToggle("ExampleToggle1", {
    Text = "Contoh Toggle",
    Default = false,
    Tooltip = "Ini toggle kosong yang bisa kamu isi nanti.",
    Callback = function(Value)
        print(Value and "Contoh Toggle aktif" or "Contoh Toggle nonaktif")
    end,
})

GroupBox1:AddInput("Textbox1", {
    Default = "",
    Numeric = false,
    Finished = false,
    Text = "Textbox 1",
    Placeholder = "Ketik sesuatu...",
    Tooltip = "Masukkan teks di sini.",
    Callback = function(Value)
        print("Textbox 1:", Value)
    end,
})

-- GroupBox 2 (Kanan)
local GroupBox2 = Tabs.Main:AddRightGroupbox("Main Options2")

GroupBox2:AddToggle("ExampleToggle2", {
    Text = "Contoh Toggle",
    Default = false,
    Tooltip = "Ini toggle kosong yang bisa kamu isi nanti.",
    Callback = function(Value)
        print(Value and "Contoh Toggle aktif" or "Contoh Toggle nonaktif")
    end,
})

GroupBox2:AddInput("Textbox2", {
    Default = "",
    Numeric = false,
    Finished = false,
    Text = "Textbox 2",
    Placeholder = "Ketik sesuatu juga...",
    Tooltip = "Masukkan teks lainnya.",
    Callback = function(Value)
        print("Textbox 2:", Value)
    end,
})

-- Theme Manager tetap bisa dipakai
Library.ToggleKeybind = Options.MenuKeybind
ThemeManager:SetLibrary(Library)
ThemeManager:SetFolder("MyScriptHub")
