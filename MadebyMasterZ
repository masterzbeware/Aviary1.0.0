local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

local Options = Library.Options
local Toggles = Library.Toggles

Library.ForceCheckbox = false
Library.ShowToggleFrameInKeybinds = true

local Window = Library:CreateWindow({
    Title = "Made by MasterZ",
    Footer = "v1.0.0",
    Icon = 0,
    NotifySide = "Right",
    ShowCustomCursor = true,
})

local Tabs = {
    Main = Window:AddTab("Main", "user"),
}

-- Groupbox kiri: Main Options1
local LeftGroupBox = Tabs.Main:AddLeftGroupbox("Main Options1")

-- Groupbox kanan: Main Options2
local RightGroupBox = Tabs.Main:AddRightGroupbox("Main Options2")

-- Toggle di Main Options1
LeftGroupBox:AddToggle("ExampleToggle", {
    Text = "Contoh Toggle",
    Default = false,
    Tooltip = "Ini toggle kosong yang bisa kamu isi nanti.",
    Callback = function(Value)
        if Value then
            print("Contoh Toggle aktif")
        else
            print("Contoh Toggle nonaktif")
        end
    end,
})

-- TextBox1 di Main Options1
LeftGroupBox:AddInput("TextBox1", {
    Default = "",
    Numeric = false,
    Finished = false,
    Text = "Textbox 1",
    Tooltip = "Masukkan teks untuk keperluan pertama",
    Placeholder = "Ketik sesuatu...",
    Callback = function(Value)
        print("Isi TextBox 1:", Value)
    end,
})

-- TextBox2 di Main Options2
RightGroupBox:AddInput("TextBox2", {
    Default = "",
    Numeric = false,
    Finished = false,
    Text = "Textbox 2",
    Tooltip = "Masukkan teks untuk keperluan kedua",
    Placeholder = "Ketik sesuatu juga...",
    Callback = function(Value)
        print("Isi TextBox 2:", Value)
    end,
})

-- Theme dan Save
Library.ToggleKeybind = Options.MenuKeybind
ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)

SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })

ThemeManager:SetFolder("MyScriptHub")
SaveManager:SetFolder("MyScriptHub/specific-game")
SaveManager:SetSubFolder("specific-place")
